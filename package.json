{
  "name": "szczepan.me",
  "version": "0.0.1",
  "scripts": {
    "test": "jest --env=jsdom"
  },
  "jest": {
    "transformIgnorePatterns": [
      "node_modules"
    ]
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^23.2.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
